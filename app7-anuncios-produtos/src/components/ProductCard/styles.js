import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  card: {
    width: 120,
    marginRight: 10,
    padding: 10,
    backgroundColor: '#eee',
    borderRadius: 8,
    alignItems: 'center',
  },
  image: {
    width: 100,
    height: 100,
    marginBottom: 5,
  },
});

export default styles